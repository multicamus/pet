<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mutli.com.pet.review">
	
	<select id="reviewList" resultType="review">
		select * from review
	</select>

	<insert id="insert" parameterType="review">
		INSERT into review (review_no, member_id, write_date_rv, review)
		VALUES (review_seq.nextval, #{member_id}, sysdate, #{review})
	</insert>

	<select id="read" parameterType="String" resultType="review">
		SELECT * from review
		WHERE review_no = #{review_no}
	</select>
	
	<update id="update" parameterType="review">
		UPDATE review
		SET	(modify_date_rv, review, review_file)
		  = (#{modify_date_rv}, #{review}, #{review_file})
		WHERE review_no = #{review_no}
	</update>
	
	<delete id="delete" parameterType="String">
		DELETE review
		WHERE review_no = #{review_no}
	</delete>
	
</mapper>